import React from 'react';

import Layout from '@/components/layout/intro';

import styles from './contact.module.scss';

const Contact = () => {
  return (
    <Layout>
      <section className={styles.container}>연락처</section>
    </Layout>
  );
};

export default Contact;
